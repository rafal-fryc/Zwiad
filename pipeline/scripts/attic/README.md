# Attic

Retired one-shot scripts kept for historical reference. Do not run these.

- `fix-researcher-outputs.py` — one-shot repair for run 2026-04-12T21-18-15
  (hardcoded run ID and finding→path map). It also emits a flat `data` shape
  that fails the current `researcher.jq` schema, so it must not be reused as-is.
