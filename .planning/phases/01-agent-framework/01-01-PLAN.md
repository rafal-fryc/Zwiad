---
phase: 01-agent-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/orchestrator.md
  - .claude/agents/scanner.md
  - .claude/agents/researcher.md
  - .claude/agents/reviewer.md
  - .claude/agents/categorizer.md
  - pipeline/runs/.gitkeep
  - reports/privacy/.gitkeep
  - reports/cybersecurity/.gitkeep
  - reports/ai-law/.gitkeep
  - input/.gitkeep
autonomous: true
requirements:
  - PIPE-05
must_haves:
  truths:
    - "Five agent definition files exist in .claude/agents/ with correct frontmatter"
    - "Each agent has the correct model assignment per D-01"
    - "Each agent has the correct tool allowlist per D-02"
    - "Directory structure for pipeline runs, reports, and input exists"
  artifacts:
    - path: ".claude/agents/orchestrator.md"
      provides: "Main pipeline orchestrator agent definition"
      contains: "model: sonnet"
    - path: ".claude/agents/scanner.md"
      provides: "Scanner subagent definition"
      contains: "model: sonnet"
    - path: ".claude/agents/researcher.md"
      provides: "Researcher subagent definition"
      contains: "model: opus"
    - path: ".claude/agents/reviewer.md"
      provides: "Reviewer subagent definition"
      contains: "model: opus"
    - path: ".claude/agents/categorizer.md"
      provides: "Categorizer subagent definition"
      contains: "model: sonnet"
  key_links:
    - from: ".claude/agents/orchestrator.md"
      to: "scanner, researcher, reviewer, categorizer"
      via: "Agent tool restrictions in frontmatter"
      pattern: "Agent\\(scanner, researcher, reviewer, categorizer\\)"
---

<objective>
Create the five Claude Code subagent definition files and the project directory structure.

Purpose: Establishes the agent subprocess infrastructure (PIPE-05) so that every subsequent phase can define agent-specific logic against a stable foundation. Also creates the directory layout for pipeline runs, reports, and input per decisions D-13 through D-16.

Output: Five `.claude/agents/*.md` files with real frontmatter and placeholder prompts, plus empty directory structure for pipeline, reports, and input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-agent-framework/01-CONTEXT.md
@.planning/phases/01-agent-framework/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project directory structure</name>
  <files>
    pipeline/runs/.gitkeep,
    pipeline/schemas/.gitkeep,
    pipeline/scripts/.gitkeep,
    reports/privacy/.gitkeep,
    reports/cybersecurity/.gitkeep,
    reports/ai-law/.gitkeep,
    input/.gitkeep
  </files>
  <read_first>
    CLAUDE.md (for project structure conventions)
  </read_first>
  <action>
Create the following directory tree with empty .gitkeep files so git tracks them:

    pipeline/
      runs/.gitkeep
      schemas/.gitkeep
      scripts/.gitkeep
    reports/
      privacy/.gitkeep
      cybersecurity/.gitkeep
      ai-law/.gitkeep
    input/.gitkeep

Use `mkdir -p` for each directory and `touch` for each .gitkeep file. Per D-13, pipeline/runs/ will hold timestamped run directories (e.g., pipeline/runs/2026-04-06T14-30-00/). Per D-14, reports/ has three topic subdirectories. Per D-15, input/ holds email digest files. Per D-16, pipeline/schemas/ holds JSON schema files (created in Plan 02).
  </action>
  <verify>
    <automated>test -d pipeline/runs && test -d pipeline/schemas && test -d pipeline/scripts && test -d reports/privacy && test -d reports/cybersecurity && test -d reports/ai-law && test -d input && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `ls pipeline/` shows directories: runs, schemas, scripts
    - `ls reports/` shows directories: privacy, cybersecurity, ai-law
    - `ls input/` succeeds (directory exists)
    - Each directory contains a .gitkeep file
  </acceptance_criteria>
  <done>All seven directories exist with .gitkeep files</done>
</task>

<task type="auto">
  <name>Task 2: Create five agent definition files with frontmatter</name>
  <files>
    .claude/agents/orchestrator.md,
    .claude/agents/scanner.md,
    .claude/agents/researcher.md,
    .claude/agents/reviewer.md,
    .claude/agents/categorizer.md
  </files>
  <read_first>
    CLAUDE.md (for agent definition examples and CLI patterns),
    .planning/phases/01-agent-framework/01-RESEARCH.md (for verified agent definition format and code examples)
  </read_first>
  <action>
Create five agent definition files in `.claude/agents/`. Each file is a markdown file with YAML frontmatter (between triple-dash delimiters) followed by a placeholder system prompt body. Per D-04, these are stubs with real frontmatter but placeholder prompts. Later phases fill in domain-specific prompts.

Each file MUST have YAML frontmatter with these fields: name, description, tools, model. Do NOT include maxTurns, budget, or permissionMode fields (per D-03).

**orchestrator.md** — Per D-01 (sonnet) and D-09 (spawns subagents via Agent tool):
  Frontmatter fields:
    name: orchestrator
    description: Coordinates the Zwiad regulatory monitoring pipeline. Spawns scanner, researcher, reviewer, and categorizer subagents in sequence.
    tools: Agent(scanner, researcher, reviewer, categorizer), Read, Write, Bash, Glob, Grep
    model: sonnet
  Body: "You are the Zwiad pipeline orchestrator. Your job is to coordinate the regulatory monitoring pipeline by spawning subagents in sequence and managing file-based handoffs between them.

Read CLAUDE.md for project context before proceeding.

[Placeholder: Full orchestration logic will be defined in Phase 5]"

**scanner.md** — Per D-01 (sonnet), D-02 (WebSearch, WebFetch, Read, Write):
  Frontmatter fields:
    name: scanner
    description: Scans regulatory sources and email digests for new privacy, cybersecurity, and AI law developments. Use when the pipeline needs to identify new items.
    tools: WebSearch, WebFetch, Read, Write
    model: sonnet
  Body: "You are the Zwiad scanner agent. Your job is to identify new regulatory developments from provided sources.

Read CLAUDE.md for project context before proceeding.

[Placeholder: Full scanning logic will be defined in Phase 2]"

**researcher.md** — Per D-01 (opus), D-02 (WebSearch, WebFetch, Read, Write):
  Frontmatter fields:
    name: researcher
    description: Researches approved regulatory findings in depth and produces structured markdown reports with citations. Use after human-approved findings are ready.
    tools: WebSearch, WebFetch, Read, Write
    model: opus
  Body: "You are the Zwiad researcher agent. Your job is to produce detailed, source-cited reports on regulatory developments.

Read CLAUDE.md for project context before proceeding.

[Placeholder: Full research logic will be defined in Phase 3]"

**reviewer.md** — Per D-01 (opus), D-02 (Read, WebFetch only):
  Frontmatter fields:
    name: reviewer
    description: Independently fact-checks research reports by verifying claims against cited sources. Use after researcher produces a report.
    tools: Read, WebFetch
    model: opus
  Body: "You are the Zwiad reviewer agent. Your job is to verify factual claims in research reports against their cited sources.

Read CLAUDE.md for project context before proceeding.

[Placeholder: Full review logic will be defined in Phase 4]"

**categorizer.md** — Per D-01 (sonnet), D-02 (Read, Write, Glob only):
  Frontmatter fields:
    name: categorizer
    description: Files verified reports into the correct topic folders with emergent subcategories. Use after a report passes verification.
    tools: Read, Write, Glob
    model: sonnet
  Body: "You are the Zwiad categorizer agent. Your job is to file verified reports into the correct topic directory.

Read CLAUDE.md for project context before proceeding.

[Placeholder: Full categorization logic will be defined in Phase 5]"
  </action>
  <verify>
    <automated>for agent in orchestrator scanner researcher reviewer categorizer; do test -f ".claude/agents/${agent}.md" && echo "EXISTS: ${agent}.md" || echo "MISSING: ${agent}.md"; done && grep -c "model: opus" .claude/agents/researcher.md .claude/agents/reviewer.md && grep -c "model: sonnet" .claude/agents/orchestrator.md .claude/agents/scanner.md .claude/agents/categorizer.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.claude/agents/orchestrator.md` exists and contains `model: sonnet` and `tools: Agent(scanner, researcher, reviewer, categorizer), Read, Write, Bash, Glob, Grep`
    - File `.claude/agents/scanner.md` exists and contains `model: sonnet` and `tools: WebSearch, WebFetch, Read, Write`
    - File `.claude/agents/researcher.md` exists and contains `model: opus` and `tools: WebSearch, WebFetch, Read, Write`
    - File `.claude/agents/reviewer.md` exists and contains `model: opus` and `tools: Read, WebFetch`
    - File `.claude/agents/categorizer.md` exists and contains `model: sonnet` and `tools: Read, Write, Glob`
    - No agent file contains `maxTurns` or `budget` fields (per D-03)
    - Each agent body contains "Read CLAUDE.md for project context"
    - grep -l "maxTurns" .claude/agents/*.md returns no results
  </acceptance_criteria>
  <done>Five agent definition files exist with correct model assignments (D-01), tool allowlists (D-02), no budget/turn limits (D-03), and placeholder prompts (D-04)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent tool restrictions | Each agent's frontmatter defines which tools it can access; a misconfigured allowlist could let an agent perform unauthorized actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | Agent frontmatter tool lists | mitigate | Each agent has explicit `tools:` field per D-02 — reviewer gets Read+WebFetch only, categorizer gets Read+Write+Glob only. Verify in acceptance criteria that no agent has tools beyond its allowlist. |
| T-01-02 | Spoofing | Agent file existence | mitigate | Research pitfall #1: `--agent nonexistent` silently succeeds. Plan 03 test suite includes agent existence preflight check. |
</threat_model>

<verification>
- All five agent files exist in `.claude/agents/`
- Model assignments match D-01 (opus for researcher/reviewer, sonnet for scanner/categorizer/orchestrator)
- Tool lists match D-02 exactly
- No maxTurns or budget fields present (D-03)
- Directory structure matches D-13 through D-16
</verification>

<success_criteria>
- `ls .claude/agents/` shows exactly: categorizer.md, orchestrator.md, researcher.md, reviewer.md, scanner.md
- `ls pipeline/` shows: runs, schemas, scripts
- `ls reports/` shows: ai-law, cybersecurity, privacy
- `ls input/` succeeds
- `grep "model: opus" .claude/agents/researcher.md .claude/agents/reviewer.md` returns 2 matches
- `grep "model: sonnet" .claude/agents/orchestrator.md .claude/agents/scanner.md .claude/agents/categorizer.md` returns 3 matches
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-framework/01-01-SUMMARY.md`
</output>
