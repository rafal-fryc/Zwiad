---
phase: 02-scanner
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/config/sources.json
  - .claude/agents/scanner.md
  - tests/fixtures/valid-scanner-output.json
autonomous: true
gap_closure: true
requirements:
  - SCAN-03

must_haves:
  truths:
    - "sources.json contains search_queries targeting law firm client alerts independent of the Lexology digest"
    - "scanner.md instructs the agent to use law firm search queries for supplemental WebSearch per D-07"
    - "tests/fixtures/valid-scanner-output.json passes scanner schema validation (includes category field)"
    - "bash tests/run-all.sh exits 0 with no test failures"
  artifacts:
    - path: "pipeline/config/sources.json"
      provides: "Law firm alert search queries"
      contains: "law-firm"
    - path: ".claude/agents/scanner.md"
      provides: "Supplemental law firm WebSearch instruction"
      contains: "law firm"
    - path: "tests/fixtures/valid-scanner-output.json"
      provides: "Valid scanner output fixture with category field"
      contains: "category"
  key_links:
    - from: ".claude/agents/scanner.md"
      to: "pipeline/config/sources.json"
      via: "agent reads source config including law firm queries"
      pattern: "search_queries"
---

<objective>
Close two verification gaps from Phase 02: (1) add independent law firm alert search queries to sources.json and update scanner.md to instruct supplemental law firm WebSearch per decision D-07, and (2) fix the Phase 1 test fixture missing the required 'category' field.

Purpose: Satisfy SCAN-03 fully (scanner can find law firm alerts without a Lexology digest) and restore a clean test suite (bash tests/run-all.sh exits 0).
Output: Updated sources.json, scanner.md, and valid-scanner-output.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scanner/02-VERIFICATION.md
@pipeline/config/sources.json
@.claude/agents/scanner.md
@tests/fixtures/valid-scanner-output.json
@pipeline/schemas/scanner.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add law firm alert search queries to sources.json and update scanner.md</name>
  <files>pipeline/config/sources.json, .claude/agents/scanner.md</files>
  <read_first>
    - pipeline/config/sources.json (current source config -- has 3 direct_fetch, 5 search_queries, zero law firm entries)
    - .claude/agents/scanner.md (current agent prompt -- no mention of supplemental law firm WebSearch)
    - .planning/phases/02-scanner/02-CONTEXT.md (decision D-07: "supplement with WebSearch for firms/alerts not in the Lexology feed")
  </read_first>
  <action>
**In pipeline/config/sources.json**, add these new entries to the `search_queries` array (after the existing 5 entries):

```json
{
  "id": "law-firm-privacy-alerts",
  "query": "law firm client alert privacy data protection 2026",
  "category": ["privacy"],
  "description": "Law firm client alerts on privacy developments not in Lexology digest"
},
{
  "id": "law-firm-cybersecurity-alerts",
  "query": "law firm client alert cybersecurity data breach 2026",
  "category": ["cybersecurity"],
  "description": "Law firm client alerts on cybersecurity developments not in Lexology digest"
},
{
  "id": "law-firm-ai-alerts",
  "query": "law firm client alert artificial intelligence AI regulation 2026",
  "category": ["ai-law"],
  "description": "Law firm client alerts on AI law developments not in Lexology digest"
}
```

These 3 entries ensure the scanner can independently discover law firm alerts via WebSearch even when no Lexology digest is provided (--sources-only mode). Each covers one of the three project categories per D-07.

**In .claude/agents/scanner.md**, add a new subsection under "### 2. Source Config Scanning" after the "Search query sources" paragraph. Insert the following text before the "On source failure" paragraph:

```
**Supplemental law firm alert discovery (D-07):**
- The search_queries in sources.json include law firm alert queries (IDs starting with "law-firm-")
- These supplement the Lexology digest to catch alerts from firms not covered by the daily feed
- When processing law firm search results, prefer the original law firm publication URL over aggregator links
- Set the source field to the firm name (e.g., "Baker McKenzie" not "Google search result")
```

Do NOT modify any other part of scanner.md. Do NOT change existing source entries.
  </action>
  <verify>
    <automated>cd /home/rafal/projecty/Zwiad && jq '.sources.search_queries | map(select(.id | startswith("law-firm"))) | length' pipeline/config/sources.json | grep -q '^3$' && grep -q "law-firm" .claude/agents/scanner.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/config/sources.json contains exactly 3 new search_queries entries with IDs "law-firm-privacy-alerts", "law-firm-cybersecurity-alerts", "law-firm-ai-alerts"
    - Each law-firm entry has a "query" string containing "law firm client alert" and a year
    - Each law-firm entry has a "category" array with the appropriate category
    - .claude/agents/scanner.md contains the text "Supplemental law firm alert discovery (D-07)"
    - .claude/agents/scanner.md mentions "law-firm-" query IDs
    - Existing sources.json entries and scanner.md content are unchanged
  </acceptance_criteria>
  <done>sources.json has independent law firm search queries covering all 3 categories; scanner.md instructs the agent to use them per D-07</done>
</task>

<task type="auto">
  <name>Task 2: Fix Phase 1 test fixture missing category field</name>
  <files>tests/fixtures/valid-scanner-output.json</files>
  <read_first>
    - tests/fixtures/valid-scanner-output.json (current fixture -- finding object lacks "category" field)
    - pipeline/schemas/scanner.schema.json (schema requires "category" with enum: privacy, cybersecurity, ai-law)
  </read_first>
  <action>
Edit tests/fixtures/valid-scanner-output.json to add `"category": "privacy"` to the finding object. The field should be added after the "development_type" field (last in the object before the closing brace).

The finding object should end up with these fields: id, title, source, source_url, summary, relevance, jurisdiction, development_type, category.

The exact line to add (after `"development_type": "legislation"`):
```
        "category": "privacy"
```

This is a one-line fix. Do NOT change any other field values or structure.
  </action>
  <verify>
    <automated>cd /home/rafal/projecty/Zwiad && bash tests/run-all.sh && echo "ALL TESTS PASS" || echo "TEST FAILURE"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/valid-scanner-output.json contains `"category": "privacy"` on the finding object
    - jq validation: `jq '.data.findings[0].category' tests/fixtures/valid-scanner-output.json` outputs "privacy"
    - bash tests/run-all.sh exits 0 with no test failures
    - test-schema-validation.sh Test 2 passes (valid scanner output validates against schema)
  </acceptance_criteria>
  <done>Phase 1 fixture updated; full test suite passes cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSearch results | Search queries return untrusted external content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | search query results | accept | Search queries return publicly indexed web content processed by Claude agent in sandboxed context; no code execution from results. Same trust model as existing government search queries. |
</threat_model>

<verification>
1. `jq '.sources.search_queries | map(select(.id | startswith("law-firm"))) | length' pipeline/config/sources.json` returns 3
2. `grep -c "Supplemental law firm" .claude/agents/scanner.md` returns 1
3. `jq '.data.findings[0].category' tests/fixtures/valid-scanner-output.json` returns "privacy"
4. `bash tests/run-all.sh` exits 0
</verification>

<success_criteria>
- SCAN-03 fully satisfied: scanner can discover law firm alerts independently via WebSearch queries in sources.json, without requiring a Lexology digest
- Test suite clean: bash tests/run-all.sh exits 0 with all tests passing
- No scope creep: only the two identified gaps are addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-scanner/02-04-SUMMARY.md`
</output>
